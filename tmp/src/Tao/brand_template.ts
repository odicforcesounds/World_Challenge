/*
 * class brand
 */
console.log("what is the minimum data that is required as company information")

class Brand {
  constructor(name, logoImg, nrEmployees, employeesList, points) {
    this.name = name;
    this.logoImg = logoImg;
    this.nrEmployees = nrEmployees;
    this.employeesList = employeesList[${user_template}]
    this.points = points; 
  }
  brandRate() {
    console.log(`The Brand: ${this.name} with ${this.nrEmployees} have ${this.points} which represent ${this.points} actions that benefit a third person.`)
  }
}

module.export Brand; 

